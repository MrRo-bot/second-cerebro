const AIChat = () => {
  //   const [messages, action, isPending] = useActionState(async (prev, form) => {
  //   const relevantNotes = await semanticSearch(form.get("q")!, userId);
  //   const context = relevantNotes.map(n => n.content).join("\n\n");
  //   const stream = await openai.chat.completions.create({
  //     model: "gpt-4o",
  //     stream: true,
  //     messages: [{ role: "system", content: `You are a helpful second brain. Use only this context:\n${context}` }, ...prev]
  //   });
  //   // stream back with React 19 <Activity> or useOptimistic
  // });
  return <></>;
};

export default AIChat;
